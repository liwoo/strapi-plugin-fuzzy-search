import { FilteredEntry, Result } from '../interfaces/interfaces';
declare const _default: ({ model, keys, query, result, }: {
    model: FilteredEntry;
    keys: string[];
    query: string;
    result: Result;
}) => Result;
export default _default;
